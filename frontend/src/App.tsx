import React from "react";

function App(): React.JSX.Element {
  return (
    <div style={{ fontFamily: "system-ui, sans-serif", padding: 24 }}>
      <h1>Raco Hotels</h1>
      <p>Frontend is running (Vite + React + TS).</p>
    </div>
  );
}

export default App;
